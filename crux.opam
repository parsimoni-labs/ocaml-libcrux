# This file is generated by dune, edit dune-project instead
opam-version: "2.0"
version: "0.1"
synopsis: "Post-quantum DSA functions with libcrux"
maintainer: ["Franziskus Kiefer" "Virgile Robles"]
depends: [
  "dune" {>= "3.0"}
  "conf-rust" {build}
  "odoc" {with-doc}
]
build: [
  ["dune" "subst"] {dev}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@runtest" {with-test}
    "@doc" {with-doc}
  ]
]
